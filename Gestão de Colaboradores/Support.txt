//Quando não tem nenhum formulário no Painel, adicionamos o primeiro. 
            //Adicionando formulário interno ao Painel.
            if (panelPrincipal.Visible == false)
            {
                //Add novo formulário.
                _Form = new Novo()
                {
                    TopLevel = false,
                    FormBorderStyle = FormBorderStyle.None,
                    Dock = DockStyle.Fill
                };  
                //Painel de Inserção do Formulário
                panelPrincipal.Controls.Add(_Form);
                _Form.ShowDialog();
                panelPrincipal.Visible = true;
            }
            //Quando já se encontra um formulário,
            //feichamos e adicionamos outro.
            else
            {
                _Form.Close();
                //Add novo formulário.
                _Form = new Novo()
                {
                    TopLevel = false,
                    FormBorderStyle = FormBorderStyle.None,
                    Dock = DockStyle.Fill
                };
                
                panelPrincipal.Controls.Add(_Form);
                _Form.ShowDialog();